name: Apply credit footer

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - name: Credit is maintained in page source
        run: echo "Creator credits are maintained directly in the HTML pages."
